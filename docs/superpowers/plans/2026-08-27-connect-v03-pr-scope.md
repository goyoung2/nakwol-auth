# Connect v0.3 PR scope

This PR intentionally batches AUTH/Connect and DATA changes so the release is verified in one review cycle rather than a sequence of micro Actions runs. Expected PR checks: one NAKWOL AUTH verify run and one NAKWOL DATA verify run.
